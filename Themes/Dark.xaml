<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:controls="clr-namespace:Automatization.Controls">

    <Color x:Key="WindowBackgroundColor">#1e1e1e</Color>
    <Color x:Key="ForegroundColor">#e0e0e0</Color>
    <Color x:Key="BorderColor">#4e4e4e</Color>
    <Color x:Key="PanelBackgroundColor">#2d2d2d</Color>

    <Color x:Key="ButtonBackgroundColor">#3e3e3e</Color>
    <Color x:Key="ButtonForegroundColor">#e0e0e0</Color>
    <Color x:Key="ButtonBorderColor">#5e5e5e</Color>
    <Color x:Key="ButtonHoverBackgroundColor">#4e4e4e</Color>
    <Color x:Key="ButtonPressedBackgroundColor">#5e5e5e</Color>

    <Color x:Key="TextBoxBackgroundColor">#2d2d2d</Color>
    <Color x:Key="TextBoxForegroundColor">#e0e0e0</Color>
    <Color x:Key="TextBoxBorderColor">#5e5e5e</Color>
    <Color x:Key="TextBoxFocusBorderColor">#007ACC</Color>

    <Color x:Key="GroupBoxHeaderColor">#007ACC</Color>
    <Color x:Key="GroupBoxBorderColor">#4e4e4e</Color>

    <Color x:Key="ScrollBarThumbColor">#424242</Color>
    <Color x:Key="ScrollBarThumbHoverColor">#555555</Color>

    <SolidColorBrush x:Key="WindowBackground" Color="{StaticResource WindowBackgroundColor}"/>
    <SolidColorBrush x:Key="Foreground" Color="{StaticResource ForegroundColor}"/>
    <SolidColorBrush x:Key="Border" Color="{StaticResource BorderColor}"/>
    <SolidColorBrush x:Key="PanelBackground" Color="{StaticResource PanelBackgroundColor}"/>

    <SolidColorBrush x:Key="ButtonBackground" Color="{StaticResource ButtonBackgroundColor}"/>
    <SolidColorBrush x:Key="ButtonForeground" Color="{StaticResource ButtonForegroundColor}"/>
    <SolidColorBrush x:Key="ButtonBorder" Color="{StaticResource ButtonBorderColor}"/>
    <SolidColorBrush x:Key="ButtonHoverBackground" Color="{StaticResource ButtonHoverBackgroundColor}"/>
    <SolidColorBrush x:Key="ButtonPressedBackground" Color="{StaticResource ButtonPressedBackgroundColor}"/>

    <SolidColorBrush x:Key="TextBoxBackground" Color="{StaticResource TextBoxBackgroundColor}"/>
    <SolidColorBrush x:Key="TextBoxForeground" Color="{StaticResource TextBoxForegroundColor}"/>
    <SolidColorBrush x:Key="TextBoxBorder" Color="{StaticResource TextBoxBorderColor}"/>
    <SolidColorBrush x:Key="TextBoxFocusBorder" Color="{StaticResource TextBoxFocusBorderColor}"/>

    <SolidColorBrush x:Key="GroupBoxHeader" Color="{StaticResource GroupBoxHeaderColor}"/>
    <SolidColorBrush x:Key="GroupBoxBorder" Color="{StaticResource GroupBoxBorderColor}"/>

    <SolidColorBrush x:Key="ScrollBarThumb" Color="{StaticResource ScrollBarThumbColor}"/>
    <SolidColorBrush x:Key="ScrollBarThumbHover" Color="{StaticResource ScrollBarThumbHoverColor}"/>

    <!-- Default Style for Labels -->
    <Style TargetType="Label">
        <Setter Property="Foreground" Value="{StaticResource Foreground}"/>
    </Style>

    <!-- Default Style for TextBlocks -->
    <Style TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource Foreground}"/>
    </Style>
    
    <!-- Default Style for TextBox -->
    <Style TargetType="TextBox">
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="5"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <Border Background="{TemplateBinding Background}" 
                            BorderBrush="{TemplateBinding BorderBrush}" 
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="3"> <!-- Slightly rounded corners -->
                        <ScrollViewer x:Name="PART_ContentHost"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="controls:HotKeyBox">
        <Setter Property="Background" Value="{DynamicResource TextBoxBackground}" />
        <Setter Property="Foreground" Value="{DynamicResource TextBoxForeground}" />
        <Setter Property="BorderBrush" Value="{DynamicResource TextBoxBorder}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="5" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="controls:HotKeyBox">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="5">
                        <ScrollViewer x:Name="PART_ContentHost" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsFocused" Value="True">
                            <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource TextBoxFocusBorder}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

        <!-- Default Style for Button -->
        <Style TargetType="Button">
            <Setter Property="Background" Value="{StaticResource ButtonBackground}"/>
            <Setter Property="Foreground" Value="{StaticResource ButtonForeground}"/>
            <Setter Property="BorderBrush" Value="{StaticResource ButtonBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3">
                            <Border.Effect>
                                <DropShadowEffect ShadowDepth="1" Direction="270" Color="Black" Opacity="0.3" BlurRadius="2"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <ColorAnimation Storyboard.TargetName="ButtonBorder"
                                                            Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                                            To="{StaticResource ButtonHoverBackgroundColor}" Duration="0:0:0.1"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <ColorAnimation Storyboard.TargetName="ButtonBorder"
                                                            Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                                            To="{StaticResource ButtonBackgroundColor}" Duration="0:0:0.1"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="{StaticResource ButtonPressedBackground}"/>
                                <Setter TargetName="ButtonBorder" Property="Border.Effect">
                                    <Setter.Value>
                                        <DropShadowEffect ShadowDepth="0.5" Direction="270" Color="Black" Opacity="0.3" BlurRadius="1"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    <!-- Default Style for RadioButton -->
    <Style TargetType="RadioButton">
        <Setter Property="Foreground" Value="{StaticResource Foreground}"/>
        <Setter Property="Margin" Value="0,0,5,0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RadioButton">
                    <Grid VerticalAlignment="Center" HorizontalAlignment="Left">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- RadioButton visual -->
                        <Border x:Name="OuterCircle"
                                BorderBrush="{StaticResource Border}"
                                BorderThickness="1.5"
                                Background="Transparent"
                                CornerRadius="8" Width="16" Height="16"
                                VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        <Ellipse x:Name="InnerCircle"
                                 Fill="{StaticResource TextBoxFocusBorder}"
                                 Width="8" Height="8"
                                 VerticalAlignment="Center" HorizontalAlignment="Center"
                                 Visibility="Collapsed"/>

                        <!-- Content presenter for text -->
                        <ContentPresenter Grid.Column="1" Margin="5,0,0,0"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"/>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="OuterCircle" Property="BorderBrush" Value="{StaticResource ButtonHoverBackground}"/>
                        </Trigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="InnerCircle" Property="Visibility" Value="Visible"/>
                        </Trigger>
                        <Trigger Property="IsChecked" Value="False">
                            <Setter TargetName="InnerCircle" Property="Visibility" Value="Collapsed"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="{StaticResource GroupBoxBorder}"/>
                            <Setter TargetName="OuterCircle" Property="BorderBrush" Value="{StaticResource GroupBoxBorder}"/>
                            <Setter TargetName="InnerCircle" Property="Fill" Value="{StaticResource GroupBoxBorder}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>