<ui:FluentWindow
    x:Class="Automatization.UI.LanguagePickerWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="Select Language"
    Width="400"
    Height="500"
    ExtendsContentIntoTitleBar="True"
    ResizeMode="NoResize"
    WindowBackdropType="Mica"
    WindowStartupLocation="CenterOwner"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ui:TitleBar Title="Select Language" Grid.Row="0" />

        <ui:TextBox
            x:Name="TxtSearch"
            Grid.Row="1"
            Margin="15,10,15,10"
            Icon="{ui:SymbolIcon Search24}"
            PlaceholderText="Search language (e.g. Romanian, ro-RO)..."
            TextChanged="TxtSearch_TextChanged" />

        <ListBox
            x:Name="LanguagesList"
            Grid.Row="2"
            Margin="15,0,15,10"
            DisplayMemberPath="DisplayName"
            MouseDoubleClick="LstLanguages_MouseDoubleClick"
            SelectionMode="Single"
            VirtualizingPanel.IsVirtualizing="True" />

        <StackPanel
            Grid.Row="3"
            Margin="15"
            HorizontalAlignment="Right"
            Orientation="Horizontal">
            <ui:Button
                Margin="0,0,10,0"
                Appearance="Secondary"
                Click="BtnCancel_Click"
                Content="Cancel" />
            <ui:Button
                Appearance="Primary"
                Click="BtnSelect_Click"
                Content="Select" />
        </StackPanel>
    </Grid>
</ui:FluentWindow>
