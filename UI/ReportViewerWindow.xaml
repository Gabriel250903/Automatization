<ui:FluentWindow
    x:Class="Automatization.UI.ReportViewerWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Automatization.UI"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="Submitted Reports"
    Width="600"
    Height="450"
    ExtendsContentIntoTitleBar="True"
    Loaded="ReportViewerWindow_Loaded"
    WindowStartupLocation="CenterOwner"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ui:TitleBar Title="{DynamicResource Settings_ViewReports}" Grid.Row="0" />

        <DataGrid
            x:Name="ReportsDataGrid"
            Grid.Row="1"
            Margin="10"
            AutoGenerateColumns="False"
            IsReadOnly="True"
            ItemsSource="{Binding Reports}">
            <DataGrid.Columns>
                <DataGridTextColumn
                    Width="Auto"
                    Binding="{Binding IsIdea, Converter={StaticResource BooleanToReportTypeConverter}}"
                    Header="Type" />
                <DataGridTextColumn
                    Width="Auto"
                    Binding="{Binding Title}"
                    Header="Title" />
                <DataGridTextColumn
                    Width="Auto"
                    Binding="{Binding Timestamp}"
                    Header="Timestamp" />
                <DataGridTemplateColumn Width="Auto" Header="Actions">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <ui:Button
                                Command="{Binding RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}, Path=DataContext.ViewReportDetailsCommand}"
                                CommandParameter="{Binding .}"
                                Content="View Details"
                                Icon="{ui:SymbolIcon DocumentOnePage24}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel
            Grid.Row="2"
            Margin="10"
            HorizontalAlignment="Right"
            Orientation="Horizontal">
            <ui:Button
                x:Name="ReportNewIssueButton"
                MinWidth="150"
                Margin="0,0,10,0"
                Click="ReportNewIssueButton_Click"
                Content="{DynamicResource Settings_ReportIssue}"
                Icon="{ui:SymbolIcon Add24}" />
            <Button
                Width="80"
                Click="CloseButton_Click"
                Content="Close" />
        </StackPanel>
    </Grid>
</ui:FluentWindow>